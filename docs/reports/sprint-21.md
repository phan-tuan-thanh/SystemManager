## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |—## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ố## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ắ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ầ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ú## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |✅## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ổ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |G## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |>## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |X## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |â## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ơ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ấ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ì## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ợ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ổ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ằ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |Đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ặ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ờ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ữ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ổ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ô## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ờ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |â## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |J## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ữ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ổ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |J## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ữ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ổ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ồ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ỹ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ầ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |J## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |â## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |j## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |J## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ẩ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ể## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ờ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ị## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ổ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ờ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ị## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ờ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |G## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |K## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ấ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |"## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |"## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ù## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ô## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ỏ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |í## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ã## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ị## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ữ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ừ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ề## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ì## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ầ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ầ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ề## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |X## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |Đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ỏ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |Q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ề## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |X## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |4## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |Q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ì## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ặ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ẫ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ì## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |G## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ú## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |J## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ể## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ố## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |í## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |J## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ầ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |é## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ỏ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ố## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ặ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ù## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ỏ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ì## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |7## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ỹ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ẩ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/changesets/:id/apply` | `POST` | Duyệt lệnh thực thi trên DB | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useApplyChangeSetMutation()` | `POST /api/v1/changesets/:id/apply` | Xác nhận thay đổi qua nút Apply. |