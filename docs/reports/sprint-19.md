## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |9## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |—## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |Q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |â## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ề## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ắ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ầ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ú## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |7## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |✅## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ổ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |G## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |>## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |X## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |â## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ố## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ắ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ờ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ể## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ặ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ô## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |â## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ề## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ò## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |Đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ặ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ờ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ữ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ợ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ủ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ô## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ờ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |z## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ù## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ồ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |K## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ồ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ể## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ỳ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ọ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |í## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ố## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |K## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |W## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |Y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |W## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |G## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ằ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ố## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |Q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ị## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |í## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ấ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ấ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ể## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ồ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ỹ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ầ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |G## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ỉ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ở## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ặ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ỉ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ấ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |=## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ờ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |í## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ị## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ấ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ầ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ờ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |Z## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ợ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ể## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ị## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ằ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ắ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ú## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ễ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ì## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ấ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ì## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ú## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |"## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |"## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ỉ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ì## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ã## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ủ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ứ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ề## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ú## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ị## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ẩ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |Q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ề## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |X## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |4## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ố## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ọ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |G## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ẽ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |é## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |4## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |4## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ề## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ô## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ồ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ẫ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ì## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |G## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ú## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ẩ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ọ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |Y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ứ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |Z## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |=## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ô## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ể## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |â## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ặ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ầ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ố## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |7## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |X## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |X## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ỹ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ẩ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/firewall-rules` | `POST` | Đề xuất luật tường lửa mới | `ADMIN`, `OPERATOR` |
| `/api/v1/firewall-rules/:id/approve` | `PATCH` | Phê duyệt hiệu lực luật | `ADMIN` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useCreateFirewallRuleMutation()` | `POST /api/v1/firewall-rules` | Submit form tạo mới luật. |
| `useApproveRuleMutation()` | `PATCH /api/v1/firewall-rules/:id/approve` | Nút bấm duyệt dành riêng cho Admin. |