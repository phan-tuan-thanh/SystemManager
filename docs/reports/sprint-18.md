## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |8## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |—## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ắ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ầ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ú## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |7## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |✅## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ổ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |G## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |>## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |X## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ứ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ằ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ữ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ợ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ấ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |W## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |z## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ỏ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ẻ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ă## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |Đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ặ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ờ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ữ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ằ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ố## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ở## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ắ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |í## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ợ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |"## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |8## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |8## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |4## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |4## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |"## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ồ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ỹ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ầ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |Đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ể## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ù## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ú## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ề## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ổ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ặ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ù## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |\## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |+## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |G## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |?## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |[## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |^## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |\## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |]## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ò## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ặ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ô## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |j## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ể## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ù## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ủ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |í## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ợ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ổ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ừ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |í## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ô## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ờ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |"## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ấ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |"## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ố## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ầ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ợ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ấ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ấ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ú## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ú## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ồ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ọ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |Q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ổ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ê## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ẩ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |%## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |F## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ỏ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ẩ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ẳ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |3## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ể## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |Q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ề## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |?## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |=## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. ||## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |X## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ử## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |N## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |E## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |L## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ộ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ụ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |8## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |X## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ẽ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ô## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ợ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |w## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ẽ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ị## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ỏ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |á## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |"## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |đ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ị## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ạ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |"## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |H## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ẫ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |B## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ì## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |b## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |G## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |/## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ú## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |*## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ẩ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ọ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |v## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ý## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ự## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ắ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ò## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |\## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |\## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ô## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |V## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ứ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ỗ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ó## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ể## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ả## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |q## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |é## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ế## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ô## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |`## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ư## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ớ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |### 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |7## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |.## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |M## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |&## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |S## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |o## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |1## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |x## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |r## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |f## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |U## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |I## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |,## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |s## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |m## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |T## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |l## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |i## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ệ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |k## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ỹ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |c## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |u## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ẩ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |P## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |O## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |D## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |(## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |A## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |R## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |e## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |a## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |d## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |)## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |C## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |p## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |h## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |ậ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |t## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |n## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |g## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |à## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |y## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |:## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. | ## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |6## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |5## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |-## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |0## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |2## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |_## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |## 4. Đặc tả API Interfaces

### 4.1. Backend Endpoints
| Endpoint | Method | Chức năng chính | Quyền hạn (Roles) |
|---|---|---|---|
| `/api/v1/import/preview?type=deployment` | `POST` | Phân tích file deployment kèm regex bóc port | `OPERATOR` |

### 4.2. Frontend Services / Hooks

| Hook / Service | API Tương ứng | Chức năng chính |
|---|---|---|
| `useUploadImportFileMutation(type)` | `POST /api/v1/import/preview` | Mở rộng truyền type định danh loại entity. |